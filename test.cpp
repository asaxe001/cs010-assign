#include <iostream>
#include <cstdlib>
#include <iomanip>
#include <cmath>
#include <ctime>
using namespace std;

int main()
{
    // srand(333);
    int roll=1, sum=0, n=20;
    srand(time(0));
    while(roll != 0 && (sum<n))
        {

        }







    return 0;
}